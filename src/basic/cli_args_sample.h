#ifndef CLI_ARGS_SAMPLE_H
#define CLI_ARGS_SAMPLE_H

/**
 * @brief CLI arguments and I/O redirection: argc/argv parsing,
 *         stdin/stdout/stderr, pipe concepts, input simulation.
 *
 * Demonstrates argument parsing with simulated data since
 * main_cli_args_sample is called from the coordinator (not from main()).
 */

int main_cli_args_sample(void);

#endif /* CLI_ARGS_SAMPLE_H */
