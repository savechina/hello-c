#ifndef STRINGS_SAMPLE_H
#define STRINGS_SAMPLE_H

/**
 * @brief Strings deep-dive: null-terminated strings, <string.h> functions,
 *         safe vs unsafe patterns, practical string parsing.
 *
 * All string operations in working code use bounded variants (strncpy, snprintf).
 * Unsafe functions (strcpy, sprintf) appear ONLY as ❌ error examples in comments.
 */

int main_strings_sample(void);

#endif /* STRINGS_SAMPLE_H */
