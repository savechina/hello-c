#ifndef SYSTEM_CLI_SAMPLE_H
#define SYSTEM_CLI_SAMPLE_H

/**
 * @brief CLI Development Patterns — CLI 开发模式
 *
 * Demonstrates:
 *   1. getopt/argc/argv — option parsing
 *   2. Exit codes — different codes for different errors
 *   3. Args validation — check required args, show usage
 *
 * Called from main_advance() → hello.c → main.c
 */
int main_system_cli_sample(void);

#endif /* SYSTEM_CLI_SAMPLE_H */
